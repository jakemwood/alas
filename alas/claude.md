# Claude Instructions

## Building

Use `just build` instead of direct cargo commands when building the project.

## Testing

Use `just test` instead of direct cargo commands when testing the project.